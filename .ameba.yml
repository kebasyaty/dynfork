# This configuration file was generated by `ameba --gen-config`
# on 2024-01-30 12:56:51 UTC using Ameba version 1.6.1.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Run `ameba --only Lint/NotNil` for details
Lint/NotNil:
  Description: Identifies usage of `not_nil!` calls
  Excluded:
    - spec/crymon/model/check/all_fields_default_spec.cr
    - spec/crymon/model/migration_spec.cr
    - spec/dependencies/mongo_client_spec.cr
    - src/crymon/fields/hash.cr
    - src/crymon/migration.cr
    - src/crymon/paladins/groups/group_03.cr
    - src/crymon/paladins/groups/group_10.cr
    - src/crymon/paladins/groups/group_11.cr
    - src/crymon/paladins/check.cr
    - src/crymon/paladins/check_plus.cr
    - src/crymon/paladins/password.cr
    - src/crymon/model.cr
  Enabled: true
  Severity: Warning

# Run `ameba --only Lint/SpecFilename` for details
Lint/SpecFilename:
  Description: Enforces spec filenames to have `_spec` suffix
  IgnoredDirs:
    - spec/helper
  Enabled: true
  Severity: Warning

# Run `ameba --only Metrics/CyclomaticComplexity` for details
Metrics/CyclomaticComplexity:
  Description: Disallows methods with a cyclomatic complexity higher than `MaxComplexity`
  MaxComplexity: 25
  Enabled: true
  Severity: Warning
